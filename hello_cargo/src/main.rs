/**
 * cargo check -> checkup code
 * cargo run -> compile unotimized .exe and startup it
 * cargo build -> compile unoptimized .exe
 * cargo build --release -> compile optimized .exe
 */
fn main() {
    println!("Hello, world!");
}
